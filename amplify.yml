version: 1
frontend:
  phases:
    preBuild:
      commands:
        - python3 -m pip install - requirements.txt
    build:
      commands:
        - python3 kaktos.py build
  artifacts:
    baseDirectory: build
