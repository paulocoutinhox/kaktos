version: 1
frontend:
  phases:
    preBuild:
      commands:
        - python3 -m pip install -r requirements.txt --user
    build:
      commands:
        - python3 kaktos.py build
  artifacts:
    baseDirectory: build
